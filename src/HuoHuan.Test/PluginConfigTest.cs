using HuoHuan.DataBase.Services;
using HuoHuan.Plugin;
using Moq;

namespace HuoHuan.Test
{
    public class Tests
    {
        [SetUp]
        public void Setup()
        {


        }

        [Test]
        public void TestLoad()
        {
            //var load = PluginConfig.Load();
            //Assert.That(load, Is.EqualTo(null));
            //var list = new List<PluginConfigItem> { new PluginConfigItem() { Name = "test" } };
            //PluginConfig.Save(list);
            //load = PluginConfig.Load();
            //if (load != null && load.Count   == 1 && load[1].Name.Equals("test"))
            //{
            //    Assert.Pass();
            //}
            //else
            //{
            //    Assert.Fail();
            //}
        }
    }
}